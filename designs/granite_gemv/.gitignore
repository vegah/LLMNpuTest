# Entry points are generated by granite_gemv.py, one per (tiles-per-call, batch,
# group). Committing them would mean a diff every time a shape or batch changes,
# and they carry no information the generator does not.
granite_gemv_p*_k*.cc
granite_null_p*_k*.cc
